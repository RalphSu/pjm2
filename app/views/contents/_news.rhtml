<h2> <%=l(:label_news)%></h2>

<% form_tag({}, :method => :get) do %>

<label for='category'><%= l(:label_category) %> :</label>
<%= select_tag 'category', news_release_option_for_select(@category), :class => "large", :onchange => "this.form.submit(); return false;"  %>
<div class="contextual">
<%= link_to l(:label_news_new), {:controller => 'news_release', :action => 'new', :project_id=> "#{@p.identifier}", :category =>"#{@category}"}, :class => 'icon icon-add' %>
</div>
</fieldset>
<% end %>

<% unless @p.nil? %>
	<table>
		<thead>
			
<!-- 			<th>
				
			</th> -->
		</thread>
	</table>
<% end %>

